# LovelyGit Performance Ledger

This is the durable index of LovelyGit performance work. Update the linked ledgers in the same commit as every performance change. Record the user-visible path, the optimization, evidence, memory effect, and commit. Keep rejected experiments so future work does not repeat them.

## Detailed Ledgers

- [Verified performance results](docs/performance/verified-results.md)
- [Completed optimization inventory](docs/performance/optimization-inventory.md)

Latest verified checkpoint: the compiled desktop navigates in 145.8 ms, idles at 9.40 MB post-GC page heap, and uses 309.45 MB private memory across the full seven-process WebView2 tree; the LovelyGit host accounts for 53.37 MB. Theme catalog bootstrap now retains 27.9 kB instead of 397.3 kB and executes in 0.068 ms instead of 0.79 ms before selected-theme realization. Warm repository-tab content activates in 7.2-13.2 ms using page-side timestamps. Maximum known-repository surfaces now retain 11 tab buttons and 15 recent rows in the real 1,000-record WebView fixture instead of mounting every record; isolated mount latency falls from 412.0 to 43.2 ms for tabs and from 541.8 to 49.0 ms for recent repositories. The same 1,000-record command palette opens in 32.6 ms instead of 72.8 ms and retains 14 result buttons instead of 1,012. Maximum branch comparisons and multi-commit confirmations now retain 14 and seven rows in the real desktop instead of all 100; end-to-end opening takes 83.1 ms and 21.4 ms respectively. Maximum Apply Patch previews now retain nine rows instead of 100 and expose every parsed file, reducing isolated mount latency from 42.9 to 34.5 ms and retained component nodes from 425 to 70. The Worktrees accordion retains 10 virtual rows instead of all 201 in its fixture, reducing the page from 3,405 to 1,124 DOM nodes and from 21.80 MB to 10.16 MB post-GC heap. Unchanged native metadata refresh for 601 refs and 201 worktrees now takes 21.3-21.9 ms per read, including 8.3-8.8 ms for worktree metadata. A 5,001-commit unchanged-path history retains 21.98 MB instead of 28.62 MB while preserving its roughly 470-502 ms traversal time. The 10,102-branch switcher now opens in 92.4 ms with 14 mounted rows, and the merge/rebase branch picker opens in 74.2 ms through CMG (33.2 ms from a direct page-side click) with 14 rows instead of 10,101, reducing its closed-dialog DOM from 21,254 to about 1,148 nodes and page heap from roughly 120-134 MB to 19-20 MB. A 10,601-ref graph hover now retains 26 pills, 1,224 DOM nodes, and 16.39 MB page heap instead of 10,601 pills, 75,224 nodes, and 593.15 MB; commit details derives the same live refs without a backend scan and mounts only three collapsed or 14 expanded pills. A 2,000-file commit details panel now retains 14 changed-file rows instead of 16, keeps all 2,000 reachable, and ordinary commits avoid virtualizer allocation entirely. A 2,000-file stash inspection now retains 17 instead of 22 file rows, while ordinary stashes avoid virtualizer allocation and tracked/untracked models use one exact-size array. Its 100,000-line modified-file view switches Combined, Side-by-Side, Full-file, Changes, and wrapping in 1.1-6.1 ms; bounded compressed-source reuse cuts the whitespace-policy reload from 626.3 to 233.7 ms without retaining the 10 MB source strings, while observed active page heap remains 34.94 MB. Compact ref transport reduces the native envelope from 1,105,168 rejected characters to 66,043 characters. A 5,000-file checkout paints loading feedback in 17.3-42.2 ms, while its variable filesystem completion remains inside Git. A fresh 20-repository desktop audit settles at 13.79 MB observed page heap, 363.22 MB full-tree private memory, and 3,889 handles after 60 tab activations, essentially matching its 364.17 MB / 3,888-handle baseline instead of retaining per repository. The expanded 1,042-test backend gate has one 57.56-second pass but currently varies above the one-minute target, so scheduler work remains open without weakening budgets. The 100,000-line diff and 20,000-file observed-path improvements remain recorded in the linked ledgers.

Maximum conflict recovery now stays inside both native-bridge directions. A 100,000-line, 2,001-file merge previously produced a 1,873,251-character response that timed out beyond 30 seconds; optimal source-bundle compression delivers a 626,495-character envelope and opens the real merge tool in 610.5-685.2 ms. Saving the 100,000-line resolved output now gzip-compacts the request to 329,813 characters, awaits the actual native mutation instead of reporting dispatch as success, stages in 631.9 ms, and continues the 2,001-file merge in 472.0 ms. Base-line anchored synchronization also keeps unequal sparse/dense Changes panes at the same conflict rather than mapping one side to the bottom of the file. The completed journey retains 11.88 MB observed page heap, leaves no conflict markers or unmerged stages, and produces an exact clean two-parent merge.

Test-gate update: after extending the suite to 1,043 tests, two consecutive clean testhosts complete in 59.20 and 58.94 seconds. This restores the under-one-minute contract without changing the 12-thread scheduler, weakening timing/allocation limits, or removing real-Git assertions.

Native command responders are now resolved from an O(1) command catalog on first use instead of constructing all 92 responders while dependency injection creates the resolver. On the same compiled empty-state launch, process-start-to-first-contentful-paint falls from 1,199.3 ms to 1,064.8-1,082.7 ms and host private memory falls from 40.09 MB to 38.00-38.50 MB. A 5,001-commit packed repository with 2,002 files still reaches first paint in 1,180.3 ms versus the 1,167 ms committed baseline, within run-to-run noise, so the startup work was not displaced into graph activation. The real WebView opened the lazily resolved remote manager correctly with clean diagnostics.

Packaged database preparation now overlaps Velopack, web-host builder creation, and dependency registration on a short-lived dedicated startup worker, then rejoins before the application is built. In alternating exact-package trials, median process-start-to-first-contentful-paint improves from 1,005.9 to 971.5 ms with the active packed 5,001-commit / 2,002-file repository, and from 1,014.8 to 994.5 ms across fresh empty databases. The 34.4 ms warm-active and 20.3 ms fresh-database savings do not defer repository work: traced graph delivery remains effectively neutral at 977 ms before and 984 ms after in representative runs. Median host private memory remains within 0.3 MB measurement noise.

Rapid appearance-colour editing now updates the UI immediately but persists only the final value after a short idle boundary. A 100-value real-WebView2 colour gesture falls from 100 native messages / 11,100 bridge bytes to one 111-byte bulk message while painting the final colour in 10.7 ms. Bulk setting persistence now validates every value before mutation and commits the complete patch in one BLite transaction: 100 four-value writes fall from a 29.62 ms / 8.24 MB median to 9.27 ms / 2.34 MB, reducing latency 68.7% and allocation 71.6%. Pending colour values flush on page exit and are cancelled when a selected theme supersedes them.

Ignoring an untracked path now removes its visible row optimistically before the native write and authoritative status reconciliation. In the real desktop against a disposable 10,000-file repository, the row disappears in 4.8-6.8 ms instead of waiting 93.8 ms for the refreshed count. Failure restores the authoritative view, while a successful write followed by refresh failure preserves the valid optimistic result for retry. The implementation retains no cache and copies only the affected sorted untracked array.

Commit-identity hydration now streams Git configuration through the shared pooled line reader, ignores unrelated sections without materializing their names, URLs, refspecs, or values, replays only identity/include directives in exact order, and discovers `extensions.worktreeConfig` during the same repository-config pass. Five reads with 10,000 unrelated remotes fall from 83.47 to 20.23 ms and from 69.26 MB to 0.46 MB allocated, saving 75.8% latency and 99.3% allocation. In the real WebView, shortening the summary's sequential Motion transition to a deliberate 100 ms brings visible identity presentation from 522.9 to 176.2 ms while preserving its fade/slide aesthetic; the panel itself appears in 45.3 ms and the next painted frame in 181.2 ms.

Remote-manager list hydration now uses the same pooled span-based configuration reader instead of allocating a string for every URL, push URL, refspec, and section line. For 10,001 remotes, warmed native reads fall from a median 18.02 ms / 8.92 MB allocated to 12.00 ms / 3.89 MB, saving 33.4% latency and 56.3% allocation while retaining the required remote models. The compiled desktop presents the 10,000-remote manager in 68.8 ms instead of the prior 108.6 ms checkpoint, retains 930 DOM nodes, and reaches the exact final fetch/push URLs with clean diagnostics. Input dispatch was already 0.5 ms and busy feedback 5.4-5.5 ms; direct Git consumes 518-545 ms for a maximum-config rename, so speculative editor indexing and native config mutation were not retained.

Maximum-ref upstream assignment now synchronizes the desktop WebView's native `input` event with the searchable branch picker. Previously, typing visibly changed the field but left the original results mounted, making a distant ref unreachable; against 10,002 remote refs, the exact final ref now appears as the sole mounted option in 2.7 ms. The surrounding path was already inexpensive: the branch context menu opens in 8.7 ms, the upstream dialog in 31.1 ms, the picker in 11.1 ms, Set paints busy in 14.4 ms and completes in 96.5 ms, and Unset paints busy in 9.5 ms and completes in 79.3 ms. The virtualized picker retains only 14 options before filtering and one afterward, so the fix adds no cache or unbounded UI model.

Repeated remote pack replacement also remains bounded. Twelve real Fetch operations each transferred a newly repacked commit containing 200 files plus a 2 MiB incompressible blob in 493-1,324 ms, followed by twenty unchanged Fetch operations in 358-457 ms. The no-op phase retained a fixed 1,036-node DOM and added only 0.33 MB of JavaScript heap; host handles settled at 608-613 rather than growing per operation, and a post-run dump found only 6.08 MB of live managed objects. The native regression now repeats six pack generations through one object store and proves that exactly one pack and index remain open after every replacement.

Fetch, Pull, and Push now await their native Git process instead of reporting success after dispatch. With a deterministic 3.1-second local transport delay, their controls painted disabled in 1-5 ms and remained protected for 3.37-3.50 seconds until Git actually completed.

Successful remote operations now invalidate the active graph directly instead of waiting for the repository watcher. Pull-to-local-ref presentation fell from 186 ms after completion to 5 ms, and fetched remote refs appeared 12 ms after completion.

Frontend wall-clock performance tests now run as a sequential Vitest project after the ordinary parallel suite, preventing unrelated workers from invalidating strict latency budgets. Two complete 709-test gates passed in 50.69 and 49.76 seconds without relaxing any threshold.

The 1,024-test backend runner keeps its measured 12-thread balance while removing another 63 avoidable Git setup processes. Standards-compliant `fast-import` streams now seed twelve independent tagged histories, an ordered octopus graph, and each commit between six required pack replacements; the tests still read real Git objects, refs, commit-graph data, Git's own parent order, and every stale-pack generation. Under full-suite contention, the first two tests fall from 9.88 to 1.22 seconds combined and repeated repacking from 5.48 to 3.95 seconds. The final profiled testhost falls from 62.81 to 55.71 seconds, while two exact complete wrappers pass in 59.36 and 58.93 seconds instead of 66.80 seconds. Focused tagged-history/octopus execution falls from roughly 3.0 to 0.97 seconds. This adds no production code or retained memory and reduces transient process, handle, and filesystem contention without weakening an assertion. The prior 12-thread cap remains; eight threads underfilled the suite, 20-24 restored contention, and coarse serialization regressed it.

The expanded 1,042-test runner now reuses immutable checkout, patch, sparse-checkout, and repository-operation seeds while preserving one exclusive physical copy per test. Native assertion reads remove twelve avoidable Git processes from the remote suite, whose full-run sum fell from 16.79 to 10.28 seconds under comparable contention. A lifecycle audit removed 10,623 abandoned seed roots from `%TEMP%`; the gate now owns all seeds beneath one PID/start-time root, normalizes inherited read-only attributes once, and leaves zero roots after completion. Complete testhosts improved from the 65.13-70.81-second regression to a best 57.56 seconds, but later clean 12-thread runs remain variable at 62.22-67.78 seconds, so the one-minute target is not yet considered restored. A ten-thread experiment passed once at 57.56 seconds and then regressed to 67.82-68.16 seconds, so the established 12-thread cap remains.

The 1,043-test runner now seeds related empty/file histories in one `fast-import`, prepares ref-scope fixtures once, uses direct authoritative ref/index/config reads for assertion-only state, and avoids separate add/commit processes for tracked fixture files. Compared with the previous clean 67.78-second testhost, consecutive complete testhosts now take 59.20 and 58.94 seconds. The commit-search class fell from 18.33 seconds under full contention to 5.00-6.05 seconds; no production path, assertion, mutation invariant, or latency/allocation ceiling changed.

Interactive-rebase conflict handling is now measured at the maximum 100-commit plan against 1,001 unmerged files, including a 100,000-line file. The real desktop commits its disabled state in 7.1-8.7 ms, paints it in 13.3-16.2 ms, and reaches the paused Working Changes surface in 11,031.1 ms on the traced warm run. Its exact Git child consumes 10,903.7 ms, leaving 127.4 ms / 1.2% for LovelyGit validation, IPC, operation-state detection, and presentation. Git itself varies from 3,754.8 to 10,903.7 ms as the filesystem cache changes; the four-worker trace peak remains 50.9 MB. A real conflict regression now proves the paused state retains its recovery plan, abort restores the exact branch, HEAD, worktree, and pre-existing status, and the operation can immediately be retried. No speculative engine or worker change was retained.

Bisect failure and recovery are now measured against 2,001 changed files and a 100,000-line file. A checkout-blocking local edit fails safely in 216.3 ms, after disabled feedback at 4.6 ms and paint at 14.8 ms; Git consumes 156.3 ms, no bisect state is left behind, HEAD and the edit remain exact, and the confirmation stays retryable. After restoring the file, Start completes in 6,565.6 ms with 6,516.5 ms inside Git. The audit found that successful Start and later Good/Bad/Skip/Reset mutations left the visible graph and branch control stale; they now invalidate the repository immediately. Reset completes in 8,374.5 ms, restores `main` visibly 11.1 ms later, and spends 8,321.5 ms inside Git. The post-journey seven-process tree was 336.63 MB private with a 61.16 MB host; the refresh fix adds no retained model or cache.

Successful branch pushes now request the same authoritative repository refresh as other ref mutations. In the delayed local-transport fixture, the pushed remote branch appeared 62 ms after completion instead of 278 ms while busy feedback remained visible in 18-21 ms.

Tag push is now audited against an exclusive bare remote containing 38,541 refs. The real desktop opens its tag menu in 3.2-3.5 ms, commits busy state in 24.0-30.3 ms, paints protection in 37.8-43.8 ms, and completes a successful local transport in 3.52-5.16 seconds. A conflicting remote tag fails without moving either authoritative ref, HEAD, index, or worktree in 281.7-356.2 ms; its controls immediately re-enable and the same action retries successfully. Multiline Git failures now surface the specific `[rejected] ... (already exists)` diagnostic instead of the non-actionable `To <remote>` header, using allocation-free span scanning until the selected line is materialized. The page retained 11.35 MB observed heap / 975 DOM nodes after retry. Alternating direct trials rejected `git push --quiet`: its 4,894.81 ms average regressed the standard command's 4,692.46 ms by 4.3%, so the existing transport remains the single path.

Maximum-ref local mutations remain inexpensive: cold Create Tag opens in 12.9 ms, paints busy in 13.1 ms, completes Git in 55.5 ms, and presents the ref 60.7 ms later. Branch Rename opens in 22.4 ms, paints busy in 12.2 ms, completes in 88.8 ms, and presents the renamed ref 64.9 ms later. The audit also corrected native WebView input handling that had left Rename disabled despite visible text.

A fresh two-repository, 50,000-commit audit opens the first native graph rows in 249-290 ms. Paging through roughly 6,000 commits retains 1,710 DOM nodes and 14.90 MB post-GC page heap; returning to a cached tab takes 96.8 ms and resets its viewport to the top. Closing the final repository tab now disposes the active native graph immediately through the existing repository-setting lifecycle instead of retaining its pack reader/traversal on New Tab. The visible repository intentionally remains warm between scrolls. Commit search responds in 217.4 ms for a recent common query, including its 140 ms debounce. A complete 50,000-commit native scan now takes 0.87-0.97 s in-process and 1.45 s through the real WebView instead of 4.47-5.03 s, while allocation falls from 305-309 MB to 131-134 MB and only 13 result rows remain mounted. Closing the search cancels active work and releases partial native traversal state immediately. The complete 951-test backend gate remains below one minute.

Large branch integrations keep the desktop responsive while Git updates the worktree. In a disposable 1,000-file plus 100,000-line fixture, Merge and Rebase painted their disabled busy states in 19.4-26.9 ms and 22.8 ms respectively, remained protected until completion, and produced the exact clean Git state. Native Merge samples reported 169-189 MB host working set and 51-67 MB private memory. Git Trace2 attributes the remaining 4.4-6.5 s completion time to Git's worktree checkout inside the desktop process rather than LovelyGit lookup, transport, or graph reconciliation; forcing one checkout worker regressed Merge to 7.36 s and was rejected.

Large branch switching now guarantees that both the top selector and graph/sidebar context actions paint their protected state before entering the synchronous native bridge. In a disposable 1,001-path fixture with 1,000 separated edits in a 100,000-line file, the first frame followed the disabled commit by 1.8 ms and the second frame completed 8.2 ms after it; native checkout started only after that second frame. The compiled desktop completed in 3.35-3.91 s with Git Trace2 assigning the delay to `git switch`, while direct four-worker Git had a 1,257.0 ms median. Eight workers reached 935.1 ms directly but raised transient aggregate Git working set from roughly 61 to 98 MiB, so the four-worker low-memory default remains. A dirty overlapping edit leaves the exact branch and bytes unchanged, re-enables the selector, and permits an immediate successful retry.

Remote tracking-branch checkout now accepts the native `input` event emitted by the desktop WebView. Previously, replacing the suggested duplicate `main` name changed the visible field but left the Create action disabled; the exact `perf/remote-checkout` value now becomes actionable during the same 49.7 ms user-like typing sequence. On the 20,001-path fixture, the context menu opens in 78.1 ms, the cold dialog in 112.4 ms, busy feedback in 21.8-23.8 ms, and the exact clean tracking checkout in 21.77-22.32 seconds with 965-973 DOM nodes and 12.73-14.58 MB observed page heap. Trace2 assigns 19.79 seconds to Git's `unpack_trees` filesystem phase before its checkout workers start; direct warm fresh tracking creation takes 3.40 seconds on the same worktree. Watcher suppression moved 21.84 to 21.77 seconds and was reverted. Eight direct workers reduce alternating checkout from 3.24-4.27 to 2.80-2.99 seconds but raise aggregate peak working set from 74-76 to 114-115 MiB and private memory from 182 to 317-324 MiB, so the four-worker low-memory default remains.

Large detached-tag checkout now has the same conclusive safety and cost coverage. The target changes 5,001 of 20,001 files, including every line of a 100,000-line file. The established four-worker policy cuts direct detach from 6.10 to 2.80 seconds and the reverse switch from 10.73 to 3.86 seconds, but the real desktop remains Git-filesystem-bound at 21.72-24.07 seconds. Trace2 places 21.84 of 22.24 seconds inside Git's `unpack_trees`; the four checkout workers themselves finish in 2.26-2.31 seconds. Protected feedback appears in 23.8-32.5 ms and paints in 35.2-46.6 ms. A dirty-file failure returns in 76.3 ms without moving HEAD or changing the user's file, leaves the dialog enabled, and retries to the exact clean detached snapshot. Cancellation also performs no mutation. The settled page retains 10.96 MB observed heap / 1,044 DOM nodes and the host 59.71 MB private. Raising the global worker count remains rejected because it cannot improve the dominant pre-worker phase and materially raises transient memory.

Arbitrary commit checkout is separately verified through its commit-row context menu and confirmation rather than inferred from tag checkout. Against 20,000 tracked files, 2,002 changed paths, and a 100,000-line file, the context menu opens in 9.8 ms and the lazy dialog in 20.0-20.6 ms. A conflicting local edit rejects in 74.9 ms without moving `main` or changing the edit and leaves the dialog retryable. Successful forward/reverse detached checkouts take 9.41/8.99 seconds in the compiled desktop; protected state commits in 1.7-1.8 ms and is visible on the 4.0-6.8 ms first frame and 12.6-16.2 ms second frame. Trace2 assigns 8.85 of 8.93 seconds to Git `unpack_trees`, leaving about 64 ms for LovelyGit. Direct Git takes 5.02/2.40 seconds on the same directions; the four workers finish in at most 698.2 ms with 48.84 MiB aggregate peak working set, confirming that more workers would not fix the dominant pre-worker phase. Exact detached HEAD, unchanged branch refs, clean 20,000-file state, 8.67 MB observed page heap, 54.41 MiB host private memory, accessibility, screenshots, and diagnostics are clean; no production rewrite was justified.

Remote branch deletion now asks Git to suppress redundant successful transport output while retaining error diagnostics. Alternating local-bare-remote samples fell from 191.0 ms to 184.2 ms average. In the real compiled WebView, the 108-local-branch / 500-tag fixture opens the context menu in 3.0 ms, opens its warmed confirmation in 23.3 ms, paints protected feedback in 36.1 ms, reports success in 263.7 ms, and removes the authoritative remote ref in 313.7 ms. The local branch, HEAD, index, and worktree remain unchanged.

Local branch deletion is now fully audited against 104 local branches and 500 tags. Safe deletion opens its context menu/dialog in 9.1/42.5 ms, paints protected feedback in 40.4 ms, reports success in 93.4 ms, and removes the authoritative ref in 100.9 ms. An unmerged safe deletion fails without mutation in 77.0 ms, keeps its confirmation retryable, then force-deletes in 110.4 ms and removes the ref in 116.6 ms; the page settles at 9.09 MB post-GC heap. The real-Git regression now proves the failed attempt preserves the exact ref, HEAD, index/worktree status, and stderr before the force retry succeeds.

Global command-palette branch creation now requests the same immediate authoritative graph refresh as the top-bar and commit-context entry points. Previously, create-and-switch reported success in 106.6 ms but relied on the repository watcher for the branch ref, which appeared at 313.6 ms. The optimized compiled desktop reports success in 118.0 ms and presents the exact ref at 125.4 ms, cutting reconciliation by 188.2 ms / 60.0% and the post-success gap from 207.0 to 7.4 ms. Create without switching paints in 35.4 ms, succeeds in 95.5 ms, and presents its ref in 99.6 ms while preserving the current branch. A real duplicate-race failure settles in 62.8 ms with its draft and controls intact, then retries to exact ref presentation in 108.9 ms. Direct Git averages 52.52 ms for create-only and 91.35 ms for create-and-switch; the page retains 11.04 MB post-GC heap.

Large commit mutations use the same immediate protection. Cherry-pick, Revert, and destructive Hard Reset painted disabled feedback in 19.2 ms, 20.9 ms, and 16.2 ms while preserving confirmation and exact clean Git outcomes. Their 4.54 s, 1.18 s, and 1.09-4.30 s native completion times remain inside Git; direct four-worker Git completed the same fixtures in 0.99 s, 0.76 s, and 0.67 s. The host stayed below 205 MB working set and 67 MB private memory across the observed commands.

Apply Patch now relies on Git's atomic apply transaction instead of launching a separate `git apply --check` process before the identical real apply. A 3.97 MB patch spanning 1,001 files falls from 2,169.4 ms to 2,025.2 ms on average, saving 144.3 ms and one process launch. Multi-file regressions prove a later invalid hunk leaves an earlier valid file, the worktree, and the index unchanged in both staged and unstaged modes; reverse and pre-cancelled paths remain covered.

The expanded Apply Patch desktop audit covers a 4.50 MB patch across 1,001 files, including 1,000 separated edits in a 100,000-line file. Direct staged Git completes in 3,456.9 ms with a 6.95 MB peak working set; the production host completes in 6,960.1 ms and presents the exact 1,001 staged paths, with Working Changes opening in 341.1 ms. Apply now paints its protected state before entering the synchronous native bridge and uses the shared 120-second mutation timeout instead of 30 seconds. A repeated-patch failure remains atomic and completes in 110 ms, while bounded stderr capture reduces its repetitive roughly 92 KB diagnostic text to a 355-byte native response containing the first failing path. Watcher suppression moved success only from 6,960 to 6,704 ms within observed variance and was reverted; Trace2 attributes the remaining desktop time to Git's own patch/file-write phase.

A fresh retained-memory audit after warming four disposable repositories leaves only 8.93 MB in the managed heap and 8.48 MB in the page heap after forced collection, with 799 DOM nodes. The LovelyGit host uses 45.6-48.1 MB private and the packaged-app-equivalent host/WebView2 subtree uses 297.4 MB private; the separate `dotnet run` launcher is development overhead and is not counted. No large first-party retained graph remains, so the bounded interaction caches were preserved.

Maximum sparse-checkout mutation preparation now scans cone paths directly and sizes its bounded deduplication table once. Normalizing 100,000 paths falls from 40.02 ms / 34.17 MiB allocated to 22.02 ms / 18.36 MiB, reducing pre-Git latency 44.98% and allocation 46.27% while retaining exact validation.

Large sparse-checkout restores now use six command-local checkout workers and suppress their owned filesystem-notification storm until Git exits. In a disposable 20,001-file repository, direct Disable falls from 6,587.0 ms to 4,801.2 ms, saving 1,785.8 ms / 27.1%; transient aggregate Git working set rises from roughly 91 MiB to 112 MiB with no idle cost. Eight workers reached 3,952.3 ms but were rejected because their typical peak rose to roughly 133 MiB. The previous unbounded 20,000-path notification accumulator took 1,091.68 ms and allocated 524,688 bytes; the bounded accumulator takes 0.70 ms and 704 bytes, a 99.94% latency and 99.87% allocation reduction before also counting the eliminated per-event cancellation source and task. The real desktop paints Restore busy state in 18.7 ms / 29.3 ms by the next frame and settles the exact 20,001-file full checkout in 10.05 seconds with a 102.8 MiB transient Git peak and a 40.14 MiB host private footprint. Successful restore now closes its confirmation; failure remains open for retry.

Command-runner, CliWrap-version, stdin-mode, shell-wrapper, Git-binary, process-priority, EcoQoS, and Job Object experiments did not improve Set/Enable when Git ran as a descendant of the self-contained desktop host and were reverted. Direct Enable averages 7,471.2 ms and Apply 330.9 ms on the same fixture, while the observed desktop Enable outlier was 84,219.8 ms. Evidence points outside LovelyGit's command construction—likely machine-level treatment of that descendant process—so no unsafe second Git runner or speculative priority override was retained.

A large submodule lifecycle is now measured end to end. In the expanded 6,001-file fixture, including a 100,000-line file and 1,000 small-file edits, Initialize paints disabled feedback in 1.9 ms and reaches authoritative `Current` in 4,440.1 ms versus 6,898.2 ms for direct Git; transient Git peaks at 99.6 MiB while the host remains at 58.0 MiB private. A real large Update paints disabled in 19.0 ms / 32.2 ms by the next frame and settles in 5,292.6 ms with the existing four-worker default. Sync settles in 754.1-837.5 ms versus a 740.2 ms direct average. Deinitialize remains Git-filesystem-bound at 17.92 s in-app versus 2,425.1 ms direct: Trace2 places the gap inside `submodule--helper deinit`, while its parent-index read takes about 0.5 ms. A real missing-source Initialize failure paints busy in 17.1-18.4 ms / 28.6-32.6 ms, re-enables in 990.8-1,106.5 ms with a visible error, and leaves no checkout or partial module. Restoring the source permits an immediate in-dialog retry that settles the exact checkout in 3,695.7 ms; failure peaks at 34.8 MiB transient Git / 44.9 MiB host private and retry at 86.0 / 47.5 MiB. Watcher suppression regressed Deinitialize to 20.09 s and six checkout workers regressed Update to 5,750.1 ms while raising transient Git memory from 77.5 MiB to 103.4 MiB, so both experiments were reverted.

Maximum linked-worktree creation now uses eight checkout workers only after Git's existing 100-path parallelism threshold. Against 20,001 tracked files, including a target commit with 1,000 small-file edits and one 100,000-line addition, direct creation falls from 6.61-6.90 seconds to 4.41-4.48 seconds and the real desktop falls from 12.66 to 10.09 seconds. Busy state still disables in 3.2 ms and paints in 13.0 ms; host private memory remains 44.5 MB. The eight worker processes have a 107.4 MB aggregate peak working set versus 58.6 MB for four workers, a bounded transient trade-off with no idle cost. Sixteen workers reached 3.92 seconds directly but were rejected for doubling worker count again for only another 0.49 seconds. Lock and Unlock settle in 64.2 and 108.2 ms; removal remains inside Git's recursive deletion and was not given a second unsafe engine. Open and Remove now reconcile the repository returned by the completed mutation directly into the existing frontend list instead of hydrating all known repositories again. With 10,001 known records, this removes a mandatory 55.6 ms native round trip and 7.58 MB allocation from each registered worktree mutation; cold Open falls from 487.3 to 388.5 ms and three warm samples move from a 286.2 to 275.5 ms median while preserving 2.6-4.2 ms busy feedback. The remaining missing-path lookup measures 18.96 ms / 7.38 MB per call; a retained backend path cache was deliberately rejected until an allocation-saving database index can preserve case-insensitive legacy-path semantics without increasing idle RAM.

Git LFS mutations remain responsive with a 50,500-line `.gitattributes`: Track, Untrack, and Prune paint disabled feedback in 8.7-17.4 ms and complete through the desktop in 739, 612, and 416-450 ms. Equivalent direct Git takes 645, 472, and 352 ms, leaving only about 8 ms of measured UI/native-envelope overhead. A missing-object Fetch paints busy in 22.0/34.5 ms, fails safely in 509.4 ms at 52.3 MiB transient Git/Git-LFS and 41.9 MiB host private, then retries in-dialog in 473.0 ms at 30.7/42.8 MiB. Git LFS can print an attributes-write error yet exit zero; LovelyGit now verifies Track/Untrack against the already-required authoritative state read. A locked-file false success is rejected in 416.9 ms with busy feedback at 19.0/27.4 ms and preserves the draft, while the immediate retry applies in 875.3 ms and clears it. No duplicate scan is added. Successful prune closes its confirmation while failure stays open and retryable.

Maximum supported file blame no longer stalls on an all-different parent or silently exceeds the WebView bridge. A 50,000-line, 1.7 MB file with 10,000 alternating attribution hunks previously timed out beyond ten seconds even though direct Git blame took 3.49 seconds. The native no-common-line partition now completes in 96-165 ms, and a bounded gzip envelope carries the otherwise oversized result in 338,877 bytes. Hover preloading removes the cold dialog-chunk wait: the real desktop loads the chunk in 42.8 ms and opens the first blame in 211.2 ms total with a 137.6 ms native round trip. A typed line-offset index retains about 203 KB rather than 2.07 MB of per-line strings in isolation, while virtualization keeps 40 rows mounted and preserves exact access to line 50,000.

Deep blame now shares the dialog's actual lifetime instead of continuing after the user leaves. On the packed 100,000-commit unchanged-file fixture, closing after 150 ms previously left 4,461 ms of native traversal running and raised observed desktop private memory from 50.31 to 74.11 MB. The explicit cancel now stops that request after 247 ms / 267.9 ms from the click, avoiding about 4.21 seconds of invisible work. The dialog still closes in 145 ms, and an immediate retry resolves the exact two-line attribution in 4,485.6 ms end-to-end / 4,404 ms natively with 14.61 MB observed page heap and clean diagnostics.

Deep file history now cancels native traversal when its dialog closes instead of merely ignoring the eventual response in React. On the exclusive 100,000-commit fixture, an uninterrupted deep search takes 4,586 ms end-to-end / 4,527 ms natively; closing after 150 ms now removes the dialog in 136.8 ms and stops the native read after 239 ms / 257.1 ms from the click, avoiding roughly 4.29 seconds of otherwise invisible traversal. The same dialog immediately retries to the exact first commit in 4,340 ms, with 4,276 ms native work, one mounted result, 14.43 MB observed page heap, and clean WebView diagnostics.

Large commit patch transport is now bounded before rendering and bridge-safe when the result is usable. A single 100,000-line replacement previously built and serialized a roughly 5.3 MB patch, remained unresolved beyond 25 seconds, and raised desktop private memory to about 507 MB. It now returns a 341-byte truncation response in 110 ms and settles the UI in 174.6 ms with 39.4 MB host private memory. A valid 1,620,150-character patch compresses to 346,208 bridge characters, reaches the clipboard exactly in 141 ms, and a valid two-commit 2,160,827-character series compresses to 462,080 characters and copies in oldest-to-newest order in 180.3 ms. Commits above the 200-file copy limit now skip unusable patch generation and return in 8.6 ms native / 83.5 ms UI.

Undo Last Commit is also conclusively measured rather than changed speculatively. Against 20,000 tracked files, a latest commit changing 1,001 files and adding one 100,000-line file previews in 110.6 ms, paints its disabled state 5.6 ms after the actual click, performs the atomic ref update in 59 ms, refreshes all staged changes in 62-82 ms, and settles in 318.8 ms. Direct `git update-ref` took 53.3 ms, so the existing native-read plus single CLI mutation path was retained.

Large Commit and Amend no longer make Git calculate and buffer a success summary that LovelyGit discards. Against 2,001 staged files including 1,000 separated edits in a 100,000-line file, controlled direct Trace2 falls from 260.8 to 195.9 ms with `commit --quiet`, saving 64.9 ms / 24.9%, while Git peak working set falls from 46.75 to 19.08 MB, saving 27.67 MB / 59.2%. A reverse-order confirmation measured quiet at 192.7 ms / 19.09 MB and the default at 424.3 ms / 46.99 MB. The optimized desktop paints busy in 5.6/15.9 ms and settles the exact clean commit in 203.4 ms with 61.5 ms inside Git and a 15.3 MB Git peak; an equally wide Amend settles in 214.2 ms with 70.4 ms inside Git and a 14.72 MB peak. A real signing failure remains visible and retryable without changing HEAD or any of the 2,001 staged paths, and `--quiet` preserves stderr diagnostics.

Large remote transport is now measured with an exclusive local bare remote containing 5,000 files and successive 64 MiB incompressible blobs. Full clone opens in 4.53 s through the desktop versus roughly 5.0 s through direct Git, reports its first progress in 2.5 ms, and cancels to a clean destination in 91.6 ms after the request. Fetch presents the new commit in 988.5 ms versus 1,201.3 ms directly. Warm Push paints disabled in 2.2-3.5 ms and settles in 1.35-1.37 s versus 1.45-1.66 s through direct Git; an initial 7.26 s cold sample did not reproduce and remains recorded as filesystem/pack-cache variance. Pull remains variable at 5.01-5.59 s versus 1.49 s directly, but Trace2 places 6.41 s inside Git's own unpack/checkout region rather than LovelyGit transport or reconciliation. Deferring or fully disabling worktree watcher notifications during Pull did not improve it and was reverted. The host remained about 44-63 MB private during Fetch/Pull and about 122-126 MB during the warmed Push fixture; transient Git processes account for the large-operation peaks.

Guarded force push is now audited end-to-end rather than only at its confirmation overlay. In the compiled WebView2 app, a disposable 20,000-file repository with a rewritten 2,001-file commit opens the push menu in 1.6 ms and the lazy force-with-lease dialog in 12.5 ms. A deliberately stale lease is rejected without remote mutation in 127.7 ms and restores both dialog controls by 197.8 ms; after restoring only the disposable lease, the same UI transfers the previously absent commit objects, reconciles exact local/tracking/remote refs, and settles in 375.8-377.4 ms. Both controls are disabled within 25 ms, the final worktree is clean, observed page heap is 10.67 MB, and page/error-console gates are clean. No production rewrite was retained because the measured path is already comfortably responsive.

New-repository initialization now reconciles the repository returned by the successful native command directly into frontend state instead of immediately requesting the complete persisted repository list again. In the real WebView2 app, warm median dialog completion falls from 237.9 to 225.2 ms and the initial commit appears in 251.0 ms instead of 259.2 ms; protected feedback remains immediate at 2.1 ms. Direct Git init plus the required empty first commit takes a 221.1 ms median, so those two process launches remain the dominant cost. Exact branch, commit, clean state, visual output, and diagnostics were verified in disposable repositories.

Maximum remote-manager transport is now bridge-safe as well as virtualized. Returning 10,000 configured remotes as the ordinary JSON response previously exceeded the native bridge and timed out after 12 seconds with no rows. Large lists now use the shared bounded gzip envelope while small lists keep the allocation-free direct path. The same real WebView2 manager opens in 108.6 ms, retains eight remote rows / 994 DOM nodes at the end of the list, uses 14.3 MB of page heap, reaches `remote-09999` exactly, and passes clean page-error and console gates.

Remote updates now scan only the named config section instead of materializing and sorting every configured remote before launching Git. Against a 10,001-remote config, the prerequisite read falls from 15.30 ms / 8,904,808 allocated bytes to 3.74 ms / 97,128 bytes, reducing measured latency 75.6% and allocation 98.9%. The real 10,000-remote desktop opens Edit in 4.8 ms and paints protected feedback in 4.9 ms; total Update remains Git-process-bound at 993.1 ms versus the 985.0 ms baseline, so no end-to-end speedup is claimed. The manager retained 938 DOM nodes and 11.21 MB observed page heap, applied the exact fetch/push URLs, and passed clean diagnostics.

Large branch comparisons no longer stop at the first 500 changed files. A 2,001-file divergent comparison now carries every file in a 12,492-byte gzip envelope, completes its real desktop native round trip in 46.5 ms, and retains only 18 file actions / 917 DOM nodes while scrolling the full 80,034 px range. Its 100,000-line diff completes in 394.5 ms end-to-end, retains 44 virtual rows / 1,424 DOM nodes, and settles at 28.81 MB post-GC page heap. Cached layout controls switch in 22.3-103.2 ms; whitespace-policy reloads take 219.3-238.8 ms. A second end-to-end audit with 2,002 changed files and 100 sparse changes in the large file opened the dialog in 94.3 ms, the file list in 18.5 ms, and the diff in 344.0 ms. Every display control settled within 20.2-82.5 ms except the native whitespace reload at 201.6 ms; forced collection after closing left 9.57 MB page heap versus a 7.43 MB clean-restart baseline. The complete frontend gate passes 732 tests in 48.43 seconds and the backend wrapper remains below one minute at 57.3 seconds.

Exact commit comparison now uses the same immediate deferred-overlay reveal as branch comparison instead of React's delayed Suspense fallback. Against two divergent commits changing 2,001 files and one 100,000-line file, the first real desktop surface falls from 366.6 to 94.1 ms and has the complete virtualized file list ready in 107.2 ms. Opening the large exact-commit diff settles in 502.7 ms with 1,448 DOM nodes. Side-by-side, Combined, Changes, Full file, wrapping, and whitespace controls complete in 118-204 ms while preserving line numbers and add/remove colors; the dialog remains a separate 5.59 kB lazy chunk.

Remote-tag deletion now uses the immediate deferred-overlay path instead of the last delayed graph-dialog Suspense boundary. In a disposable 500-tag repository backed by a local bare remote, the cold confirmation enters the real desktop DOM in 17.6 ms instead of 311.3 ms and reaches the next painted frame in 23.2 ms instead of 316.7 ms, while retaining its existing 100 ms entrance animation and separate 1.72 kB lazy chunk. The principal destructive action paints busy feedback in 17.6 ms and completes in 241.8 ms; direct Git verification proves the remote tag is gone, the local tag remains, and the worktree is clean.

Local-tag deletion is now conclusively audited rather than changed speculatively. Against 500 tags, direct `git tag --delete` has a 43.3 ms median; the real desktop opens its context menu in 15.1 ms and warmed confirmation in 23.5 ms, paints protected feedback in 38.4 ms, completes in 65.0 ms, and removes the authoritative ref in 70.6 ms. A simulated missing-ref race fails safely in 60.9 ms without closing the dialog, then retries to success/ref removal in 62.8/68.2 ms. Cancellation preserves the tag, the page retains 13.64 MB observed heap and 727 DOM nodes, and clean diagnostics plus exact ref/worktree checks pass. Replacing standard Git deletion with silent `update-ref` semantics was rejected because the small process-bound latency does not justify weakening missing-tag diagnostics.

Partial line and hunk staging now preserves the authoritative old/new line endings instead of building LF-only patches. The former 100,000-line CRLF action failed outright; accepting it with Git's whitespace override was rejected because it silently produced mixed-EOL blobs. Uniform files add only two default-ending fields and zero per-line objects, while mixed files use sorted compact integer overrides. In the 20,001-file desktop fixture, line/hunk Stage completes natively in 73-78 ms and Unstage in 58-60 ms; optimistic diff feedback appears in 6.7-14.7 ms and the authoritative working-changes list settles in 128.1-149.7 ms. The 575,539-byte baseline diff response grows by only 102-119 bytes, its measured backend preparation remains 207-341 ms versus the prior 421 ms sample, and exact index-blob checks prove CRLF, mixed EOL, and missing-final-newline bytes survive Stage and Unstage.

Saved-stash restoration now suppresses Git's redundant file-by-file status output while preserving normal branch-from-stash output. Against 20,001 tracked files and a stash changing 1,001 files including 1,000 separated edits in a 100,000-line file, direct Apply falls from 1,061.6 ms to 907.9 ms on average, saving 153.7 ms / 14.5%; Pop falls from 1,003.2 ms to 927.1 ms, saving 76.1 ms / 7.6%. In the real desktop, disabled feedback appears in 1.5-3.3 ms and paints in 9.9-14.8 ms. Apply and Pop retain the exact 1,001-file result; Drop completes natively in 78 ms and leaves no stash or worktree changes. The observed Apply response used 9.47 MB managed and 39.01 MB host private memory. Trace2 attributes the remaining checkout-heavy desktop time to Git's unpack/checkout region; running the same direct command while LovelyGit watched the repository still completed in 1.10 seconds, so watcher suppression and process-priority changes were rejected rather than adding lifecycle complexity.

Large stash creation now publishes an exact optimistic working-tree projection as soon as Git succeeds and reconciles status in the background. On 20,001 tracked files with 1,001 changed paths and 1,000 separated edits in a 100,000-line file, busy feedback paints in 28.3-35.8 ms and the action settles 42-49 ms after the native response instead of awaiting a further 295-302 ms status scan, saving roughly 246-260 ms on the post-Git critical path. The preview is released after either refresh success or failure, and failed reconciliation cannot relabel a successful stash. Direct `--quiet` and watcher-suppression experiments were rejected because they did not improve Git time.

Branch from stash now treats successful Git completion as the interaction boundary and performs its authoritative working-tree reconciliation in the background. On the same 20,001-file / 1,001-change fixture, post-native settlement falls from about 356 ms to 60 ms, saving roughly 296 ms; busy feedback paints in 28-40 ms and the branch selector reflects the new branch in 470-492 ms while Git continues restoring files. Direct Git takes 3.22-3.54 s and the desktop's variable 5.35-5.61 s remains inside Git: Trace2 records 4.84 s in checkout/unpack, and direct Git remains fast while LovelyGit watches the repository, ruling out watcher contention.

Ignore completion now stops once the optimistic projection is exact and reconciles the complete status in the background. With 5,000 modified files in a 20,001-file repository, Local-ignore controls settle in 45.1 ms instead of 1,099.1 ms. Shared `.gitignore` controls now settle in 90.9 ms instead of 1,110.6 ms, saving 1,019.7 ms / 91.8%, by merging a path-scoped `.gitignore` status response before the full refresh; its row/busy/paint feedback arrives in 7.7/7.6/11.2 ms and the exact 5,001-file result remains visible. Token-safe clearing prevents either background refresh from disturbing a newer mutation. A separate archive audit retained the single direct `git archive` path: 20,001-file ZIP export averaged 463.0 ms, `-0` saved only 81.8 ms while growing output 82%, and TAR averaged 140.2 ms when users prefer speed over ZIP compression.

Mixed reflog reset now skips Git's immediate index refresh because LovelyGit follows the mutation with its authoritative working-tree scan. Against 20,001 tracked files and a commit changing 1,001 files including 1,000 separated edits in a 100,000-line file, direct Mixed reset falls from 289.9 to 254.8 ms on average, saving 35.1 ms / 12.1% while preserving exactly 1,001 unstaged and zero staged files. In the real desktop, the Resetting state commits in 16.3 ms, is observed on the next frame at 21.5 ms, and the exact Working Changes result opens in 545.3 ms with 12.76 MB active page heap. Hard reset remains unchanged: it paints protected feedback before its 6.18-second checkout and leaves the target clean. Eight checkout workers reduced direct Hard reset from 1,731.8 to 1,419.0 ms but were rejected because four extra transient Git processes conflict with the low-memory goal; `--no-refresh` also regressed Hard reset.

Startup now reconciles a persisted current-repository ID against the successfully loaded repository list before graph consumers use it. A removed/stale ID previously left the desktop in a permanent Detached HEAD graph skeleton: it still retained 1,508 DOM nodes and 33.42 MB page heap after 12.47 seconds and required a manual tab switch. The same stale database now reaches New Tab by the first CMG attachment in 350.3-474.0 ms, with 673 nodes and 9.52-9.61 MB page heap, removing 835 retained nodes / at least 23.81 MB from the broken state and persisting the repair. A valid 601-ref repository remains fast, reaching usable branch controls at 455.5 ms, 24.2 ms after attachment. Failed repository-list reads deliberately preserve the stored ID. The root chunk grows only 0.19 kB minified / 0.05 kB gzip.

Maximum known-repository hydration now keeps small lists direct and gzip-compacts lists at 512 records before crossing the native bridge. A 10,001-record list is 1,230,159 raw JSON bytes and previously exceeded the 10-second request timeout, emitted an unhandled page rejection, and left the graph without repository tabs even though BLite read it in 50.78 ms cold / 22.55 ms warm with 7.58 MB allocated per warm read. The compact contract returns 607,514 characters in 55.6 ms end-to-end with 37 ms inside the native command. Successful removal now reconciles the authoritative mutation into the existing client list instead of immediately paying for another complete native read, decompression, and replacement. At the same 10,001-record scale this eliminates a mandatory 55.6 ms / 7.58 MB hydration; repeated real-desktop removals settle in 23.4, 26.2, and 27.6 ms (26.2 ms median), down from the 86.6 ms baseline sample, while native failure retains the repository and concurrent local reconciliation is not lost. A persistent normalized path index replaces the remaining full-list existence scan: missing lookup falls from 18.96 ms / 7.38 MB to 0.0087 ms / 813 bytes and existing lookup takes 0.0111 ms / 1,541 bytes. The one-time 10,000-record legacy migration takes 279.6 ms, allocates 207.5 MB transiently, and adds 6.0 MB on disk; an upgrade-only aggressive collection returns those temporary segments, leaving a fresh-migration desktop at 66.21 MB host private versus 66.74 MB after a warm restart and 105.16 MB without reclamation. The real desktop retains only 13 tabs, 799 DOM nodes, and 15.34 MB observed page heap; New Tab opens in 109.1 ms with 15 rows, exact-last filtering takes 59.8 ms, the retained command palette opens in 104.3 ms with 14 rows, and its exact-last filter takes 79.5 ms with one row. Initial list failures are contained while preserving the stored repository ID, clean diagnostics pass, and the root bundle grows only 0.02 kB minified / 0.01 kB gzip.

Partial line and hunk staging now releases decoded full-source arrays as soon as compact delta rows have been projected. A staged four-line diff backed by two 100,000-line sources previously retained 75.53 MB of observed page heap because the release policy considered only the four rendered changes; the same desktop flow now settles at 33.94 MB, saving 41.59 MB / 55.1%. Interaction feedback remains immediate: Stage Line/Hunk paints in 28.5/27.1 ms and Unstage Line/Hunk in 25.0/26.4 ms; the measured line operations settle in 179.3/97.4 ms. Direct Git patch cost remains the floor at 59-72 ms on the 20,001-file index. Failed untracked line staging now also rolls back its temporary intent-to-add entry, matching the existing hunk safety contract.

## Measurement Rules

- Measure from a healthy runner state and use disposable repositories only.
- Use Chromium-sized repositories for large-repository checks and CMG for WebView interaction timing.
- Report cold and warm behavior when caches materially affect the result.
- Treat latency, allocations, retained memory, payload size, process count, and test duration as separate budgets.
- Do not claim a win without a before/after measurement or a focused regression benchmark.

## Mutation Workflow Baselines

Measured through the same Git commands LovelyGit uses, primarily in a disposable 2,001-file repository with an 8 MiB file; rows identify the larger 20,000-file fixture where used. These are investigation baselines, not all optimization claims.

| Workflow | Observed latency |
| --- | ---: |
| Create/delete branch or tag | 44-50 ms |
| Checkout roughly 1,000 changed paths | 0.97-1.03 s after bounded parallelization |
| Stash 501 changed files | 3.83 s cold; 1.33-1.39 s warm |
| Stash 1,001 changes across 20,001 files | 8.06-8.60 s native; 28.3-35.8 ms busy paint; 42-49 ms post-native settlement with 295-302 ms reconciliation moved off the critical path |
| Branch from a 1,001-change stash | 3.22-3.54 s direct Git; 5.35-5.61 s native desktop; post-native settlement 356 ms to 60 ms |
| Restore 501-file stash | 1.06 s |
| Create/remove 2,001-file worktree | 1.28-1.36 s / 586 ms after bounded parallelization |
| Create/remove 20,000-file worktree | 5.45 s / 6.65 s direct Git; removal busy state paints in 3.8-4.4 ms |
| Open a linked worktree with 10,001 known repositories | Cold 487.3 to 388.5 ms; warm median 286.2 to 275.5 ms; avoids a 55.6 ms / 7.58 MB follow-up hydration |
| Clone and open with 10,001 known repositories | Warm median 364.4 to 303.4 ms after local reconciliation, then 277.6 ms with indexed path lookup; cold sample 480.0 to 428.3 ms |
| Cherry-pick/revert 100 changed files | 251 ms / 183 ms |
| Merge 500 changed files | 1.02 s |
| Rebase 100 changed files | 894 ms |
| Interactive rebase of 100 commits / 1,010 files | Busy DOM 3.5 ms; visibly painted by 8.9 ms; completed in 273.5 ms versus 294.7 ms direct Git |
| Interactive rebase paused by 1,001 conflicts | Busy DOM 7.1-8.7 ms; visibly painted by 13.3-16.2 ms; 11,031.1 ms desktop completion with 10,903.7 ms inside Git and 50.9 MB transient Git peak |
| Git LFS Track / Untrack / Prune with 50,500 attributes | Busy paint 8.7-17.4 ms; desktop completion 739 / 612 / 416-450 ms; direct Git 645 / 472 / 352 ms |
| Merge / rebase 1,000 files plus one 100,000-line file | Busy paint 19.4-26.9 ms / 22.8 ms; desktop completion 4.40-4.59 s / 6.52 s; exact direct Git 0.95-1.12 s / 1.72 s. A separate 20,000-file/2,001-change merge painted busy in 4.3-5.7 ms and completed in 9.79-10.23 s versus 2.40-2.63 s direct. |
| Cherry-pick / revert / hard reset 1,000 files plus one 100,000-line file | Busy paint 19.2 ms / 20.9 ms / 16.2 ms; native completion 4.54 s / 1.18 s / 1.09-4.30 s; exact direct Git 0.99 s / 0.76 s / 0.67 s |
| Mixed reflog reset across 1,001 changed files | 289.9 ms to 254.8 ms direct; busy DOM 16.3 ms, next frame 21.5 ms, exact desktop settlement 545.3 ms |
| Bisect across 2,001 files plus one 100,000-line file | Failure busy/paint 4.6/14.8 ms and safe rejection 216.3 ms; retry 6.57 s; reset 8.37 s with visible branch refresh 11.1 ms later |
| Apply 3.97 MB / 1,001-file patch | 2,169.4 ms with redundant preflight to 2,025.2 ms atomic apply; 144.3 ms / 6.7% faster and one fewer Git process |
| Stage 1,000 of 20,000 tracked files | 7.18 s cold CMG completion; 0.70-0.90 s warm service runs |
| Unstage 1,000 of 20,000 tracked files | 749 ms CMG completion; 0.31-0.47 s warm service runs |
| Discard 1,000 of 20,000 tracked files | 87 ms warm CMG completion; 0.65-0.67 s cold service runs |
| Stage / unstage a line or hunk in a 100,000-line file | Busy paint 25.0-28.5 ms; line settlement 97.4-179.3 ms; direct Git patch 59-72 ms; staged four-line view heap 75.53 to 33.94 MB |
| Commit / Amend 2,001 staged files plus one 100,000-line file | Commit 260.8 to 195.9 ms direct and 46.75 to 19.08 MB Git peak; optimized desktop 203.4 ms; Amend 214.2 ms |
| Commit 1,000 staged files in a 20,000-file repository | 0.23-0.34 s service; 439.2 ms CMG control settlement after deferring auto-maintenance |
| Fetch / Pull / Push completion contract | Disabled feedback in 1-5 ms; awaited 3.37-3.50 s delayed transport process |
| Remote completion to authoritative refs | Pull 186 ms to 5 ms; Fetch 12 ms measured after the change |
| Branch push completion to remote ref | 278 ms to 62 ms; busy feedback remains visible in 18-21 ms |
| 500-tag create / 102-branch rename | Tag: 12.9 ms dialog, 13.1 ms busy, 55.5 ms Git, 60.7 ms reconciliation; rename: 22.4 ms dialog, 12.2 ms busy, 88.8 ms Git, 64.9 ms reconciliation |
| 50,000-commit graph retention | Cold rows 249-290 ms; roughly 6,000 paged commits retain 14.90 MB post-GC page heap and 1,710 DOM nodes; warm tab return 96.8 ms at scroll top; close-last-tab releases the native graph |
| 50,000-commit search | Recent common query 217.4 ms including 140 ms debounce; complete native traversal 4.47-4.89 s to 0.87-0.97 s and 305-309 MB to 131-134 MB allocated; real WebView 4.92-5.03 s to 1.45 s; 13 mounted rows |
| Maximum 50,000-line file blame | >10 s timeout to 96-165 ms native work and 211.2 ms first real-app open after hover preload; 338,877-byte compact envelope; 40 mounted rows |
| Undo a 1,001-file / 100,000-line commit | Busy paint 5.6 ms after actual click; 59 ms atomic undo; 62-82 ms authoritative refresh; 318.8 ms complete |
| Copy maximum commit patch / patch series | >25 s unresolved and ~507 MB private memory to 174.6 ms bounded rejection; valid 1.62M-character patch copies in 141 ms and valid 2.16M-character series in 180.3 ms |
| Clone / Fetch / Pull / Push with 64 MiB incompressible increments | Clone 4.53 s, Fetch 988.5 ms, Pull 5.01-5.59 s, Push 1.35-1.37 s; interaction feedback 2.2-3.5 ms and clone cancellation 91.6 ms |
| Open 10,000 configured remotes | >12 s bridge timeout with no rows to 108.6 ms; eight rows / 994 DOM nodes retained and 14.3 MB page heap |
| Update one of 10,001 configured remotes | Prerequisite config read 15.30 ms / 8.90 MB allocated to 3.74 ms / 97 KB; desktop feedback paints in 4.9 ms while total remains about 0.99 s inside Git-heavy mutation work |
| Open exact comparison across 2,001 files | 366.6 ms cold reveal to 94.1 ms; complete list 107.2 ms, 100,000-line diff 502.7 ms, controls 118-204 ms |
| Open/delete a remote tag among 500 tags | 311.3 ms cold DOM / 316.7 ms paint to 17.6 / 23.2 ms; deletion busy paint 17.6 ms and exact completion 241.8 ms |
| Complete backend regression gate | 1,054 tests in 64.87 s to 58.38-55.97 s; ref-heavy external-mergetool preflight fixture 2.75 s to 0.92 s |
| Hydrate/mutate among 10,001 known repositories | Hydration: >10 s timeout / missing tabs to 55.6 ms exact native round trip; removal settles at a 26.2 ms repeated median; worktree open and Clone/Open Repo reconcile authoritative responses locally instead of repeating the 55.6 ms / 7.58 MB hydration |

## Rejected or Deferred Experiments

| Experiment | Observed benefit | Reason not shipped |
| --- | --- | --- |
| Add `--quiet` to large merges | Desktop merge remained 10.11 s versus its 9.79-10.23 s baseline | Success output is not the bottleneck; reverted. |
| Replace the existing ref-view helpers with one maximum-ref projection pass | 0.3143 ms to 0.0497 ms per isolated 10,601-ref projection, saving only 0.2646 ms | The real context menu, dialog, and picker already open in 8.7, 31.1, and 11.1 ms, so the small absolute saving does not justify changing established grouping semantics. |
| Enable Git FSMonitor for checkout | Chromium checkout improved from about 2.1 s to 0.85–0.90 s warm | Persistent daemon retained about 37.8 MB per repository and the first full status scan regressed to about 3.5 s. |
| Enable Git untracked cache automatically | Warm status reached about 0.41–0.44 s | Initial warm-up was about 3.6 s and mutating user repository configuration was not justified. |
| Suppress working-tree watchers during checkout | No repeatable improvement; maximum remote tracking checkout moved only from 21.84 to 21.77 s | Added lifecycle complexity without measurable value; reverted. Trace2 keeps the delay inside Git's pre-worker `unpack_trees` filesystem phase. |
| Swap Git executable path for checkout | No repeatable improvement | Nested app-process launch cost remained; reverted. |
| Replace standard stash push with `--quiet` or create/store/reset | None; warmed standard was 1.33 s, quiet 1.37 s, custom 1.47 s | Standard Git semantics were both fastest and safest, so the alternatives were rejected. |
| Add `--quiet` or watcher suppression to the 1,001-change stash fixture | Quiet averaged 4,545.9 ms versus 4,435.2 ms standard direct Git; watcher suppression left the desktop at 8.20 s | Neither reduced mutation time, and suppression delayed the visible saved-stash card until completion, so both were removed. |
| Add `--quiet` to local branch deletion | Normal safe deletion averaged 50.80 ms; quiet averaged 61.89 ms across ten alternating samples per mode | The extra option regressed average latency and did not improve the already-sub-120 ms desktop journey, so standard Git output remains. |
| Bypass object caches while peeling loose tags | Cold Chromium-scale graph open regressed from 281.86 ms / 5.21 MB allocated to 319.37 ms / 7.55 MB | Many lightweight tags share commit targets, so the existing bounded object cache avoids repeated pack reads; retained unchanged resolver. |
| Batch submodule HEAD tree lookups with a path trie | Allocations fell from 2.53 MB to 2.18 MB for 1,000 definitions, but latency moved from 59.29 ms to 60.18 ms | Path normalization and worktree state checks dominate the manager read; the extra parser complexity did not improve interaction latency, so it was removed. |
| Add six checkout workers to large submodule Update/Initialize | Warm direct Update improved from 1,911.1 ms / 102.8 MiB peak to 1,726.1 ms / 121.4 MiB, while eight workers added no further speed | The authoritative desktop Update regressed from 5,292.6 to 5,750.1 ms and transient Git memory rose from 77.5 to 103.4 MiB, so the command-local override was removed. |
| Suppress repository watchers during submodule Deinitialize | None; desktop completion regressed from 17.92 to 20.09 s | Trace2 places the delay inside Git's recursive `submodule--helper deinit` filesystem work, not LovelyGit refresh; retained the safe Git implementation. |
| Replace async worktree fan-out with synchronous `Parallel.For` | Allocations remained about 43.5 MB and ten-read latency regressed from 304.30 ms to 325.15 ms in the intermediate small-file experiment | The existing bounded async scheduler was retained; allocation was subsequently addressed at the small-file buffer layer instead. |
| Cache worktree snapshots behind per-file metadata fingerprints | Allocations fell from about 3.77 MB to 2.39 MB, but ten unchanged reads regressed from roughly 274 ms to 333 ms | Validating hundreds of files costs nearly as much as reading them on Windows and introduces cache complexity; direct bounded reads remain faster and immediately fresh. |
| Keep only the top 100 painted branch-comparison commits during traversal | Repeated 10,000-commit runs remained at 65.85-65.99 MB and latency varied within the prior range | Sorting the displayed subset is not the bottleneck; packed commit decoding dominates, so the more complex bounded selector was removed. |
| Force the native status scanner across a 20,000-entry index | 822 ms and 10.5 MB allocated versus Git's roughly 48-53 ms warm scan | The existing 1,000-entry crossover policy remains correct; wide repositories should use Git's optimized index/stat traversal. |
| Replace streamed single-file pathspec input with a direct `git add -- <path>` argument | Both forms remained roughly 52-57 ms on the 20,000-file fixture | Git index rewrite cost dominates; retained the shared streamed pathspec path for consistent validation and batching. |
| Replace bulk `git add -A` with `git add -u` for tracked-only changes | `add -u` took about 579 ms versus 537 ms for `add -A` on the 20,000-file fixture | It was slower and would exclude untracked files from Stage All, so the existing command was retained. |
| Detach post-commit status reconciliation | No improvement; the graph updated in 441 ms but the commit control remained busy for about 1.38 s | Trace2 proved the delay was inside Git's automatic maintenance, not LovelyGit's reconciliation, so the experiment was reverted. |
| Add only `--quiet` to foreground commit | Highly variable at 192-1,095 ms versus 361-1,906 ms without it | Output was not the bottleneck; automatic maintenance dominated. LovelyGit instead disables auto-maintenance for that invocation and schedules it after success. |
| Hard-link repository-template files in the backend suite | Full-suite time regressed from 98.4 s to 100.3 s and retained the same two contention-sensitive failures | NTFS link creation and the remaining fixture setup dominated; reverted to isolated physical copies to preserve straightforward ownership. |
| Let the serialized performance collection overlap ordinary tests | Full-suite time improved from 94.7 s to 86.2 s | Still exceeded the one-minute requirement and retained timing-budget failures under contention, so global isolation was restored. |
| Increase xUnit's maximum parallel threads to eight | The complete gate exceeded 120 s | Extra process and filesystem contention made the suite slower, so the default scheduler was restored. |
| Share template Git objects through alternates | No repeatable wall-clock improvement | It changed which object store the commit-graph reader observed and complicated fixture ownership, so exclusive physical copies were retained. |
| Split performance tests into six in-process lanes | Testhost wall time reached 59.83 s | `GC.GetTotalAllocatedBytes` is process-global; concurrent measurements caused 14 false allocation/latency failures, so the isolated collection was restored. |
| Run six performance lanes in separate testhosts | The isolated lane phase completed in 24.5 s | Simultaneous Git/filesystem workloads caused three valid latency gates to fail under contention, so the safe single-testhost gate remains authoritative. |
| Increase parallel-checkout settings for stash | Cold 501-of-20,000-file stash moved only from 4.19 s to 4.10 s | LovelyGit already supplies four checkout workers; Trace2 showed the temporary-index update dominates, so no additional Git configuration was added. |
| Raise Hard reset from four to eight checkout workers | Direct 1,001-file reset improved from 1,731.8 to 1,419.0 ms | Saving 312.8 ms did not justify four additional transient Git worker processes; the established four-worker memory bound remains. |
| Add `--no-refresh` to Hard reset | Direct samples moved from 1,526.0-1,671.6 ms to 1,629.2-1,764.3 ms | Hard reset already updates the index as part of checkout, so the option regressed rather than removing duplicate work. It is scoped only to Mixed reset. |
| Raise maximum worktree creation from eight to 16 checkout workers | Direct creation improved from 4.41 s to 3.92 s | The extra eight transient Git processes saved only 0.49 s after eight workers had already removed roughly one third of the four-worker time; eight is the measured speed/memory balance. |
| Prefer Git for Windows' `cmd\\git.exe` launcher over `mingw64\\bin\\git.exe` | No improvement: the 5,000-file in-app checkout completed in 23.13 s through the launcher versus 21.46-23.02 s through the direct executable | Executable selection is not the bottleneck, so the established direct executable and helper-path discovery remain unchanged. |
| Raise global checkout workers from four to eight or sixteen | Direct 5,000-file switches improved from 2.81/3.85 s with four workers to 2.42/3.01 s with eight. At 20,001 paths, eight improves 3.24-4.27 s to 2.80-2.99 s | The extra four workers raise aggregate peak working set from 74-76 to 114-115 MiB and private memory from 182 to 317-324 MiB, yet cannot address the in-app pre-worker filesystem phase; the bounded four-worker policy remains. |
| Disable parallel checkout for large integration operations | The experiment removed transient checkout workers | The same 1,000-file Merge regressed from 4.40-4.59 s with four bounded workers to 7.36 s with one worker. Trace2 showed the single-worker checkout itself consumed the extra time, so the established four-worker policy remains. |
| Disable WebView2 GPU acceleration | Compiled private memory fell from 302.92 MB to 212.95 MB; 500-tag and 100,000-line-diff virtual scrolling retained 120 Hz / zero frames over 20 ms | Warm two-frame tab activation regressed from 16.72 ms to 18.61 ms average, large-diff scrolling was unchanged, and [Microsoft's WebView2 performance guidance](https://learn.microsoft.com/microsoft-edge/webview2/concepts/performance) says GPU rendering is critical and should only be disabled for troubleshooting. Hardware acceleration remains enabled. |
| Replace or virtualize the appearance theme selector | The real compiled WebView opens 49 light-theme options in 27.3 ms, adds only 210 DOM nodes, and remains at about 8.5 MB observed page heap | This is already comfortably inside the interaction budget and uses the design system's custom scrollbar; a heavier searchable picker would add complexity and bundle weight without a measured user-visible win. |
| Split the commit graph into an on-demand root chunk | Root JavaScript fell from 792.27 to 658.48 kB minified and empty post-GC page heap moved from 4.60 to 4.42 MB | Empty first paint regressed by roughly 8-14 ms and active-repository paint by about 18 ms while graph delivery stayed neutral at 972.0 versus 971.5 ms. The 0.18 MB heap saving did not justify slower startup, so the split was reverted. |
| Replace native deep commit search with `git log --grep` | Direct Git scans the packed 50,000-commit fixture in roughly 291-341 ms; the optimized native core is now 0.87-0.97 s instead of 4.47-4.89 s, with 1.45 s measured through the real WebView | LovelyGit's read architecture deliberately keeps commit discovery in-process, and the CLI result would discard the resumable session and native filter pipeline. The remaining gap stays visible for later profiling, but no longer justifies silently changing engines. |
| Defer or disable worktree watcher notifications during remote Pull | Pull remained 5.08-5.13 s and showed no repeatable improvement | Git Trace2 attributes the delay to Git's unpack/checkout work; both watcher experiments added lifecycle complexity and were fully reverted. |
| Batch the 10,000-record normalized-path migration into 128-entry transactions | Migration regressed from 279.6 ms / 207.5 MB allocated / 6.0 MB storage growth to 459.7 ms / 330.9 MB / 18.9 MB, with no retained-private-memory benefit | BLite's single atomic migration is faster and substantially more storage-efficient; upgrade-only aggressive collection solves transient retention instead. |

## Next Measurement Areas

- Repeat packaged startup on a genuinely slower storage tier and after safe operating-system file-cache eviction; the available machine exposes only its NVMe system volume, while alternating cold-database and warm-active NVMe packages are now measured.
